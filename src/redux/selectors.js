/** @format */

export const isLoading = (state) => state.products.isLoading;
export const products = (state) => state.products.goods;
export const likes = (state) => state.products.likes;
export const basket = (state) => state.products.basket;
export const order = (state) => state.products.order;
export const isRejected = (state) => state.products.isRegected;
